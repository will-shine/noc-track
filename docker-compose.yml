version: '3.8'

services:
  # Service Aplikasi Laravel
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: filament-app
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./:/var/www/html
    networks:
      - npm_network

  # Service Web Server (Nginx) untuk melayani file PHP
  webserver:
    image: nginx:alpine
    container_name: filament-webserver
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
      - ./docker-compose/nginx/app.conf:/etc/nginx/conf.d/default.conf 
    networks:
      - npm_network

  # Service Database
  db:
    image: mysql:8.0
    container_name: filament-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - npm_network

networks:
  npm_network:
    external: true # Pastikan ini nama network yang sama dengan Nginx Proxy Manager

volumes:
  dbdata:
    driver: local